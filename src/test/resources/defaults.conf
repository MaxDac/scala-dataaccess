conf {
  name = "dataaccess"
  messageBroker {
    hostname = "this-is-the-hostname"
    virtualHost = "this-is-the-virtualhost"
    exchangeName = "this-is-the-exchangeName"
  }
  webServer {
    hostname = "localhost"
    port = "8082"
  }
  dataAccess {
    type = "Oracle"
    connectionString = "jdbc:oracle:thin:@{ip:door}/{service}"
    user = "{user}"
    pass = "{password}"
  }
}